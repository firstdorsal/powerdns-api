image: node:latest

stages:
  - makedoc
#  - publish

makedoc:
  stage: makedoc
  before_script:
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -H 'git.y.gy' >> ~/.ssh/known_hosts
  script:
    - npm install
    - cat README_basic.md > README.md
    - node_modules/.bin/jsdoc2md index.js >> README.md
    - node_modules/.bin/jsdoc index.js -t node_modules/docdash-orange --readme README_basic.md
    - git config --global user.email "gitlab-ci@git.y.gy"
    - git config --global user.name "gitlab-ci"
    - git commit README.md -m "🤖 makedoc [skip ci]"
    - git push
#publish:
#  stage: publish
#  script:
#    - echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}'>.npmrc
#    - npm publish
